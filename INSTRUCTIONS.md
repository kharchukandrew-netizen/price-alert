# 🚀 Solana Token Price Alert - Інструкція

Цей скрипт моніторить ціну мем-токена на Solana і надсилає СИРЕНУ на твій iPhone через Pushover коли ціна досягне $0.03.

## Крок 1: Отримай Pushover ключі

1. Відкрий **Pushover** додаток на iPhone
2. Скопіюй свій **User Key** (на головному екрані)
3. Зайди на https://pushover.net/apps/build
4. Створи новий Application (назви як хочеш, наприклад "Crypto Alert")
5. Скопіюй **API Token/Key**

## Крок 2: Розгортання на Railway (безкоштовно)

### Варіант A: Через GitHub (рекомендовано)

1. Зайди на https://github.com і створи новий репозиторій
2. Завантаж туди 3 файли:
   - `solana_price_alert.py`
   - `requirements.txt`
   - `Procfile`

3. Зайди на https://railway.app
4. Натисни "Start a New Project" → "Deploy from GitHub repo"
5. Вибери свій репозиторій
6. Додай Environment Variables (Settings → Variables):
   ```
   PUSHOVER_USER_KEY = твій_user_key
   PUSHOVER_API_TOKEN = твій_api_token
   ```
7. Railway автоматично запустить скрипт!

### Варіант B: Через Railway CLI

```bash
# Встанови Railway CLI
npm install -g @railway/cli

# Залогінься
railway login

# Створи проект
railway init

# Задеплой
railway up
```

## Крок 3: Перевір що все працює

1. В Railway dashboard ти побачиш логи:
   ```
   [12:34:56] SYMBOL: $0.001234 | Target: $0.03
   ```

2. На телефоні переконайся що:
   - Pushover має дозвіл на сповіщення
   - "Critical Alerts" увімкнені для Pushover (Settings → Notifications → Pushover → Critical Alerts)

## 📱 Налаштування iPhone для ГУЧНОГО будильника

**Важливо!** Щоб сирена розбудила тебе:

1. **Settings → Notifications → Pushover**
   - Allow Notifications: ✅
   - Sounds: ✅
   - Critical Alerts: ✅ (це обійде "Не турбувати")

2. **В самому Pushover додатку:**
   - Settings → Sound → вибери "Siren" для перевірки

## ⚙️ Конфігурація скрипта

В файлі `solana_price_alert.py` можеш змінити:

```python
TOKEN_ADDRESS = "Cm6fNnMk7NfzStP9CZpsQA2v3jjzbcYGAxdJySmHpump"  # Адреса токена
TARGET_PRICE = 0.03  # Цільова ціна в USD
CHECK_INTERVAL = 30  # Як часто перевіряти (секунди)
```

## 🔧 Альтернативні хостинги (якщо Railway не підійде)

- **Render.com** - безкоштовний tier, схожий деплой
- **Fly.io** - безкоштовний tier
- **Replit** - можна запустити як background worker

## ❓ Проблеми?

**Сповіщення не приходять:**
- Перевір User Key і API Token
- Перевір що Pushover має дозвіл на сповіщення

**Скрипт падає:**
- Перевір логи в Railway dashboard
- Можливо токен ще не має пари на DEX

**Ціна не оновлюється:**
- DexScreener оновлюється кожні кілька секунд
- Перевір чи токен торгується на DEX
